[package]
name = "selvedge-local-protocol"
edition = "2024"
publish = false

[dependencies]
serde = { version = "1.0.215", features = ["derive"] }
serde_json = "1.0.145"

[lints.rust]
unsafe_code = "forbid"

[lints.clippy]
dbg_macro = "deny"
todo = "deny"
unwrap_used = "deny"
