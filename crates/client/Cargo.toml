[package]
name = "selvedge-client"
edition = "2024"
publish = false

[dependencies]
async-stream = { workspace = true }
bytes = { workspace = true }
futures-core = { workspace = true }
futures-util = { workspace = true }
http = { workspace = true }
reqwest = { version = "0.12.24", default-features = false, features = ["rustls-tls", "stream"] }
rustls-pemfile = "2.2.0"
selvedge-config = { path = "../config" }
selvedge-logging = { path = "../logging" }
serde_json = { workspace = true }
thiserror = { workspace = true }
tokio = { workspace = true, features = ["net", "sync", "time"] }
tokio-rustls = { version = "0.26.4", default-features = false, features = ["ring", "tls12"] }
tokio-tungstenite = { version = "0.28", default-features = false, features = ["handshake", "rustls-tls-webpki-roots"] }
url = { workspace = true }
webpki-roots = "1.0.6"
zstd = "0.13.3"

[dev-dependencies]
axum = { workspace = true }
rcgen = "0.14.5"
selvedge-test-support = { path = "../test-support", features = ["config", "http"] }
tempfile = { workspace = true }
tokio = { workspace = true, features = ["io-util", "macros", "net", "rt-multi-thread", "sync", "time"] }

[lints]
workspace = true
