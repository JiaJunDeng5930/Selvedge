[package]
name = "selvedge-harness"
edition = "2024"
publish = false

[dependencies]
selvedge-command-model = { path = "../command-model" }
selvedge-db = { path = "../db" }
selvedge-domain-model = { path = "../domain-model" }
selvedge-router = { path = "../router" }
rustix = { version = "1.1.4", features = ["process"] }
serde_json = "1.0.145"
tokio = { version = "1.42.0", features = ["io-util", "process", "rt", "sync", "time"] }
uuid = { version = "1.11.0", features = ["v4"] }

[dev-dependencies]
selvedge-api = { path = "../api" }
selvedge-core = { path = "../core" }
selvedge-test-support = { path = "../test-support", features = ["db-fixtures"] }
tokio = { version = "1.42.0", features = ["macros", "rt", "sync", "time"] }

[lints.rust]
unsafe_code = "forbid"

[lints.clippy]
dbg_macro = "deny"
todo = "deny"
unwrap_used = "deny"
