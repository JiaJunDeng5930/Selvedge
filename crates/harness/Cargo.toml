[package]
name = "selvedge-harness"
edition = "2024"
publish = false

[dependencies]
selvedge-command-model = { path = "../command-model" }
selvedge-domain-model = { path = "../domain-model" }
serde_json = "1.0.145"

[workspace]

[lints.rust]
unsafe_code = "forbid"

[lints.clippy]
dbg_macro = "deny"
todo = "deny"
unwrap_used = "deny"
