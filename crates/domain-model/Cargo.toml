[package]
name = "selvedge-domain-model"
edition = "2024"
publish = false

[dependencies]
serde = { workspace = true, features = ["derive"] }
serde_json = { workspace = true, features = ["arbitrary_precision"] }

[lints]
workspace = true
