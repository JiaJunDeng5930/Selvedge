[package]
name = "selvedge-domain-model"
edition = "2024"
publish = false

[dependencies]
serde = { version = "1.0.215", features = ["derive"] }

[lints.rust]
unsafe_code = "forbid"

[lints.clippy]
dbg_macro = "deny"
todo = "deny"
unwrap_used = "deny"
